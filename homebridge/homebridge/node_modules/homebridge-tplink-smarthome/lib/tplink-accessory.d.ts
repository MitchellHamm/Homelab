import { Categories } from 'homebridge';
import type { PlatformAccessory, Service, WithUUID } from 'homebridge';
import { TplinkSmarthomeConfig } from './config';
import type TplinkSmarthomePlatform from './platform';
import type { TplinkDevice } from './utils';
export default class TplinkAccessory {
    private readonly platform;
    private readonly config;
    tplinkDevice: TplinkDevice;
    readonly homebridgeAccessory: PlatformAccessory;
    private readonly log;
    private readonly hkDevice;
    private readonly services;
    private readonly name;
    private lsc;
    constructor(platform: TplinkSmarthomePlatform, config: TplinkSmarthomeConfig, homebridgeAccessory: PlatformAccessory | undefined, tplinkDevice: TplinkDevice, category: Categories, services: Array<WithUUID<typeof Service>>);
    private setupCharacteristics;
}
//# sourceMappingURL=tplink-accessory.d.ts.map